services:
  - type: web
    name: finsight-server
    runtime: node
    rootDir: server
    buildCommand: npm run build
    startCommand: npm run start:prod
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: AUTH0_DOMAIN
        sync: false
      - key: AUTH0_AUDIENCE
        sync: false
